import { Navigator } from "Navigator.slint";
import { SoreWindowGlobal, NavigatorSideButton} from "WindowGlobal.slint";

export component SoreWindow inherits Window {
    width: SoreWindowGlobal.Width;
    height: SoreWindowGlobal.Height;

    Navigator { }
}

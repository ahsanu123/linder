import { SoreWindowGlobal } from "WindowGlobal.slint";

export component SoreButton inherits Rectangle {
    background: #1B3C53;
    width: SoreWindowGlobal.SideBarWidth;
    height: SoreWindowGlobal.Height / 3;

    @children
}
